import React from 'react';
import Navbar from './Navbar';

const HeroFav = () => {
    return (
        <div>
            <Navbar/>
            <h1>Hero fav</h1>
        </div>
    );
}

export default HeroFav;
